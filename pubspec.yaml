name: skillsprint
description: SkillSprint client production-ready
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  firebase_core: ^2.14.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.8.0
  firebase_messaging: ^14.4.0
  http: ^0.13.6
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
